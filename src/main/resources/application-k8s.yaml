server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: book_route
          uri: http://book-service
          predicates:
            - Path=/books/**
          filters:
            - PrefixPath=/api/v1
